#ifndef _MAIN_H_
#define _MAIN_H_

char *create_array(unsined int size, char c);
char *_strdep(char *str);
char *str_concat(char *s1, char *s2);
int **alloc_grid(int width, int height);
void free_grid(int **grid, int height);
char *argstostr(int ac, char **av);

#endif
